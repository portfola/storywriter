import { StyleSheet } from "react-native";

const s = StyleSheet.create({
  container: {
    width: '100%',
    padding: 12,
    backgroundColor: '#2c3e50',
    alignItems: 'center',
    position: 'absolute',
    bottom: 0,
  },
  text: {
    color: '#fff',
    fontSize: 14,
  },
});

export { s };